import React from 'react';
import { Modal } from 'semantic-ui-react';

interface Props {
  required?: boolean;
  name: string;
  type?: string;
  label?: string;
  inputPlace: string;
  inputRef: any;
  error?: {
    message?: string;
  };
}

const ChangeProfile = () => {
  return (
    
  
  <div>
    sdsdsd
</div>
)
};

export default ChangeProfile;
