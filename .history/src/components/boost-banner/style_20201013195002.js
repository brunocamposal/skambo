import styled from 'styled-components';

export const MainBanner = styled.div`
  background: ${color};
  width: 900px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
`;

const Infols = styled.div`
  display: flex;
  flex-flow: row wrap;
`;
